## 1. Setting up the project

`npm install`

or

`yarn`

_yarn must be installed globally, this can be done with the command:_

`npm i -g yarn`

## 2. Running tests

`yarn test`

or

`npm test`

To run the metatest, use:

`npx vitest run supertest.test.js`
